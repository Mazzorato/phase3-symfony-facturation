{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
<section class="bg-gray-100 min-h-screen p-6">
    <h1 class="text-2xl font-bold text-gray-800 text-center mb-8 uppercase tracking-widest"></h1>
    <div class="max-x-xl mx-auto bg-white rounded-xl shadow-md p-6 mb-10">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-xl font-semibold text-gray-700">Modifier le produit</h2>   
            <div class="flex justify-between items-center mb-6">
                <a href="{{ path('app_product') }}" class="text-gray-500 hover:text-gray-700">
                    <i class="fa-solid fa-xmark"></i>
                </a>
            </div>
        </div>
    {{ include('product/_form.html.twig', {'button_label': 'Update'}) }}
    </div>




    {{ include('product/_delete_form.html.twig') }}
</section>
{% endblock %}
