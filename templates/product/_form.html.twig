{{ form_start(form) }}

    <div class="mb-4">
        {{ form_label(form.name, 'Nom', {'label_attr': {'class': 'block text-sm text-gray-600 mb-1'}}) }}
        {{ form_widget(form.name, {'attr': {'class': 'w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
    </div>

    <div class="mb-4">
        {{ form_label(form.description, 'Description', {'label_attr': {'class': 'block text-sm text-gray-600 mb-1'}}) }}
        {{ form_widget(form.description, {'attr': {'class': 'w-full border border-gray-300 rounded-lg px-3 py-2 h-24 focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
    </div>

    <div class="flex gap-4 mb-6">
        <div class="w-1/2">
            {{ form_label(form.price, 'Prix', {'label_attr': {'class': 'block text-sm text-gray-600 mb-1'}}) }}
            {{ form_widget(form.price, {'attr': {'class': 'w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
        </div>
        <div class="w-1/2">
            {{ form_label(form.unit, 'Unité', {'label_attr': {'class': 'block text-sm text-gray-600 mb-1'}}) }}
            {{ form_widget(form.unit, {'attr': {'class': 'w-full border border-gray-300 rounded-lg px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500'}}) }}
        </div>
    </div>

    <div class="flex justify-end gap-3">
        <a href="{{ path('app_product') }}" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50">Annuler</a>
        <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">Enregistrer</button>
    </div>

{{ form_end(form) }}